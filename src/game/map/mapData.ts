export { generateFullMap, generateEntities, generateClouds, generateBirds, generateFish, generateSmokeSources, generateWeatherParticles } from '../utils/mapGenerator';
export { TileType, ObjectType, EntityType } from '../types/mapTypes';
export type { GameMapData, MapObject, MapEntity, Cloud, Bird, Fish, SmokeSource, WeatherParticle, DamageData } from '../types/mapTypes';
